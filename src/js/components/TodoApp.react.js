var React = require('react');

var TodoApp = React.createClass({

  /**
   * @return {object}
   */
  render: function() {
    return (
      <div>
        Open the door Hal!
      </div>
    );
  }
});

module.exports = TodoApp;
